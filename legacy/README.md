# Legacy pipeline (archived)

This folder keeps the original “one huge `canvas.aseprite` → full RGBA buffer → raw `.bin` tiles → GitHub Pages” pipeline.

It is **not** used by Personal Pixel Studio.

See the root [README.md](../README.md) for the current local-first Studio workflow (pieces, Download HTML, Publish).
